1.Produce summary statistics, visualizations, and interpretive text describing the distributions of SalePrice, TotRmsAbvGrd, and OverallCond.
2.Separate the data into subsets based on OverallCond, then demonstrate how this split impacts the distribution of SalePrice.
3.Find the features that have the strongest positive and negative correlations with SalePrice, and produce plots representing these relationships.

Task: optional
Join basics_df to rating_df pn tconst
Find avg rating per every genre per year
Find correlation: genre vs rating(+ve/-ve?)
Plot hist of that
Make the conclusion - interpretive text

Task : Gross df
Find how to deal with Nan values- foreign gross has alot
Know the total gross  of domestic vs foreign by year.
Get the top 5 studios of domestic vs foreign respectively
Advice on competitive markets for both

Task : Subset
Categorize rating into 3: 1-4, 5-7, 8-10
find the total numvotes across each category.
Check if there is correlation between numvotes and rating. Is rating better if fewer people vote?
Show in three

Using budget:
Join budget to basic using movie_name and primary title
Find profits,loss(based on genres)


Methods/ functions we could need
for null values:
columns we need to
 drop
rename (axis=1/0...)
df['column'].fillna
isna()
dropna()(subset=['column']

dtypes
.astype({...})

bins, labels
pd.cut

create new column by split
df['newcol']=df[col'].str.split('-', expand=true)

column_to_move then insert function

replace odd characters in values
regex  function???

